//Declaring the array
let bhanu = ["good", 1 , 1.0];
console.log(bhanu);


//Another way
let m = new Array();
m[0] = "bhanu"
m[1] = 1
console.log(m);


//operation in Array
let items = ["pen", "pencil"]
items.push("laser") // Add element in the arrays at last
console.log(items);
items.pop()//used to remove the element last one
items.shift()//used to remove the first element
items.unshift()//used to add the ekement at the first






